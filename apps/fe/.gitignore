# Logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ---------------------------------
# Local dev only (not for production)
# (moved to apps/ — these paths are relative to repo root now)
# See root .gitignore for project-wide ignores

# ---------------------------------